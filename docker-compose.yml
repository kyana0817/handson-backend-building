version: '3'

services:
  auth:
    image: redis:7.0.8
    ports:
      - '6379:6379'
    command: redis-server --requirepass password
    volumes: 
      - auth:/data
    restart: "no"

volumes:
  auth:
    driver: local