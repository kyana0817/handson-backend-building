{
  "name": "handson-backend-building",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run start --workspace=client\" \"npm run dev --workspace=server\" \"npm run dev --workspace=auth\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "auth",
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  }
}
